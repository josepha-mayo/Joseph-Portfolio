Release attempt 4. Restore the exact original source.srt fixture omitted from the v1.2 archive; all 125 unchanged inherited JavaScript tests passed locally with that restored file. Build from a clean candidate directory and execute all new identity, native renderer, editing-race, cancellation and inherited ASR checks. Combine the existing full-workflow walkthrough with the new actual Source Lock recording and fully decode the result. No app security policy or inference result is bypassed.
