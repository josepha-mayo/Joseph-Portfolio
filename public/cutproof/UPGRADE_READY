CutProof v1.2 candidate, attempt 2.
Corrected the build to insert scripts only before the final real body tag, not body-tag text inside JavaScript export templates.
Repeat all new workflows with actual browser speech inference. Also rerun native renderer validation.
The submitted immutable v1.1 remains unchanged until public verification passes.
